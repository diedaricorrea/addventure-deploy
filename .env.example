# ========================================
# VARIABLES DE ENTORNO PARA DOCKER COMPOSE
# ========================================
# Copiar este archivo a .env y completar con valores reales
# NUNCA subir .env a Git (ya está en .gitignore)

# Base de Datos MySQL
MYSQL_ROOT_PASSWORD=tu-password-root-mysql-super-segura
DB_USERNAME=addventure_user
DB_PASSWORD=tu-password-mysql-segura

# JWT Secret (generar con: .\generate-jwt-secret.ps1)
JWT_SECRET=tu-clave-jwt-super-larga-y-aleatoria-minimo-64-caracteres

# Email (opcional, para recuperación de contraseñas)
MAIL_USERNAME=tu-email@gmail.com
MAIL_PASSWORD=tu-app-password-de-gmail

# CORS - Permitir acceso desde estas URLs
# Para desarrollo local: http://localhost,http://localhost:4200
# Para producción: http://tu-ip-publica,http://tudominio.com
CORS_ALLOWED_ORIGINS=http://localhost,http://localhost:4200

# WebSocket - Mismo valor que CORS
WEBSOCKET_ALLOWED_ORIGINS=http://localhost,http://localhost:4200
